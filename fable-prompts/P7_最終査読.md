# P7: 最終査読チェック（S7）

> **このセッションはAntigravity等ローカル対応エージェントを推奨**（tex全文＋実験数値をまとめて渡せる）。texは個人情報が**仮置きのまま**（個人情報はP7完了後にローカルで置換する設計なので、実名の送信は発生しない）。
> **Antigravityの場合**: レポート・予稿・スライドのtexを自分で編集した後に、3ファイルを添付し、このプロンプト本文を貼り付けて送る（記入欄部分は不要）。
> **v0.appで実施する場合**: プロンプトはGitHub取得（または貼り付け）し、編集済みtexを初回メッセージに貼る。ただしtex全文は長文のため入りきらない可能性が高く、1セッションで1ファイルずつ（例: まずレポートだけ）に分割すること。texをリポジトリに載せるかどうかは自分の判断。

---

あなたは卒業研究の中間成果物一式（レポート＋2ページ予稿＋Beamerスライド）を査読する校正担当です。以下のタスクを**一度の応答で完結**させてください。質問は禁止です。**応答はすべて日本語で**。

## タスク
下記の3つのLaTeX文書を、正確性・整合性・書式遵守の観点で点検し、修正済みの完全版を出力する。

## 点検チェックリスト（各項目について PASS/FAIL と証拠（該当箇所の引用）を報告すること）
1. **分量**: 予稿がjsarticle 2段組A4でちょうど2ページ相当（文字数から推定。コンパイル不可のため内容量で見積る）、レポートが10〜16ページ相当、スライド本編が12フレーム以内
2. **数値の整合**: 本文・予稿・スライド中のすべての数値（CVaR誤差、N、α、シード数、mean±std）が下記「【実験結果（記入欄）】」の数値と完全一致しているか。不一致をすべて列挙
3. **数式の整合**: W₁ = ∫₀¹|F⁻¹−F̂⁻¹|dτ ／ ρ_g(Z)=∫₀¹F⁻¹dg(τ) ／ 上界不等式 |ρ_g(Z)−ρ_g(Ẑ)| ≤ ∫|F⁻¹−F̂⁻¹|g′(τ)dτ = W₁(g_#Z, g_#Ẑ) ／ ∂W₁/∂τᵢ = 2F⁻¹(τᵢ)−F⁻¹(τ̂ᵢ)−F⁻¹(τ̂ᵢ₋₁) が3文書すべてで同一か。表記ゆれ・曖昧な記法を指摘
4. **引用の健全性**: 参考文献が下記ホワイトリストのみか（捏造・リスト外の引用をすべて指摘）。ホワイトリスト: FQF (Yang et al., NeurIPS 2019)／IQN (Dabney et al., ICML 2018)／QR-DQN (Dabney et al., AAAI 2018)／C51 (Bellemare et al., ICML 2017)／Beyond CVaR (Moghimi & Ku, ICML 2025)／Tail-Safe (Zhang, arXiv:2510.04555, 2025)／Graf & Luschgy (LNM 1730, Springer 2000)／Rockafellar & Uryasev (J. Risk 2(3), 2000)／Lim & Malik (NeurIPS 2022)／Yaari (Econometrica 55(1), 1987)
5. **日本語校正**: 誤字脱字・助詞の誤り・用語の不統一（例: 「分位点フラクション」vs「フラクション」vs「τ」— 統一方針を決めて適用）、LaTeXの破損（括弧・環境の不整合、\end の欠落、\includegraphics のファイル名誤り）、図表参照（図1/図2等）の整合
6. **文体**: 全編だ・である調。口語表現の混入禁止。進行中の実験について誇張がないか（「実装中」が「完了」になっていないか等）
7. **予稿固有**: 構成が「はじめに→関連技術・先行研究→研究目的→研究方針→現在までの進捗→今後の予定（月別）→参考文献(4〜6件)」になっているか、図1/図2が本文で参照されているか
8. **個人情報**: 実名等が混入していないか（【氏名】等の仮置きのままか）

## 【texファイル（記入欄1）: レポート report.tex】
（ここに report.tex の全文を貼る）

## 【texファイル（記入欄2）: 予稿 yoshi.tex】
（ここに yoshi.tex の全文を貼る）

## 【texファイル（記入欄3）: スライド slides.tex】
（ここに slides.tex の全文を貼る）

## 【実験結果（記入欄）: 数値の正】
（ここに実験結果の数値を貼る。例: 対数正規・N=16・α=0.05: 一様 0.123±0.011 → 歪み最適 0.045±0.004 ／ 混合ガウス・N=32・α=0.01: 0.210→0.031）

## 出力形式（厳守）
応答は**単一のHTMLコードブロックのみ**（自己完結・オフライン動作）。HTMLには: (1) 点検結果表（項目・PASS/FAIL・証拠・適用した修正）、(2) 修正済みの3ファイル全文（`report_fixed.tex`、`yoshi_fixed.tex`、`slides_fixed.tex`）、(3) 各ファイルと `findings.md`（点検報告のMarkdown版）のダウンロードボタン（Blob + URL.createObjectURL）。記入欄が空の場合は修正ファイルを作らず、点検結果表に「入力なし」と記して修正指示のみ出すこと（捏造禁止）。HTMLの外に説明文を書かないこと。

# slides_qa.md — 質疑応答シート（本編5分＋質疑3分）

- 本番投影スライドは **slides_main.pdf**（本編10枚のみ・QA/Appendixなし）。質疑では本シートの回答原稿を使用する。
- 深掘り対応が必要な場合は、**予備版 slides_concise.pdf**（Appendix A1〜A7 収録）を別デバイス等で開き、対応表のバックアップスライドを表示する。
- 回答は各30〜45秒。「結論 → 対比 → 締め」の順で話す。
- 話体：「〜であります」「〜と考えます」（slides_script.md と同一）。

## 質問とバックアップスライドの対応表（バックアップスライドは予備版 slides_concise.pdf 内のページ）

| 質問 | 表示するスライド |
| :--- | :--- |
| Q1 IQN との違い | A4（Q1） |
| Q2 Beyond CVaR との違い | A4（Q2） |
| Q3 重み付けの自明性 | A4（Q3） |
| Q4 実験規模の妥当性 | A5（Q4） |
| Q5 IQN が優位だった理由 | A5（Q5） |
| Q6 上界 $L_g$ の証明の核心 | A1（必要なら A2） |
| Q7 裾外の勾配が 0 で学習が止まらないか | A1（下段の注） |
| Q8 原子数 $N$ の選び方 | A3 |
| Q9 非凸性・局所解への対処 | A3・A5（Q5） |
| Q10 デルタヘッジ環境への適用計画 | 本編10枚目（まとめ） |

---

## 想定質問（本問）

### Q1. IQN の歪みサンプリングとの本質的な違いは？（表示: A4）

**回答**: ご質問ありがとうございます。IQN は $\tau$ を歪んだ分布からサンプリングする、読み出し側の確率操作であります。分位点関数の表現自体は $\tau$ の全域を対象としています。一方、本研究は有限個の quantile fraction という表現力そのものを、損失の上界に基づいて下側裾に配分します。歪みをどこで扱うかが本質的に異なると考えます。

**補足**: 実装上も異なり、提案は FQF の境界勾配に $g'(\tau_i)$ を乗じるのみで、ネットワーク構造の変更は不要です（本編6枚目の定理）。

### Q2. Beyond CVaR との違いは？（表示: A4）

**回答**: Beyond CVaR は quantile fraction を固定したまま、原子の値に対してスペクトル的な重み付けを行う手法であります。本研究は区切り位置 $\tau_i$ そのものを学習し、配置を最適化します。両者は直交する設計軸でありますので、併用によりさらなる改善が期待できると考えております。

**補足**: 比較表（本編7枚目）のとおり、変更する対象が「値側」か「損失側」かの違いであり、CVaR誤差上界を扱うのは本研究のみです。

### Q3. 目的関数の重み付けは自明ではないか？（表示: A4）

**回答**: 重み付けの発想自体は自然であると認識しております。しかし我々の調査した範囲では、損失側で quantile fraction を歪み最適化した先行研究は見当たりませんでした。本研究の特徴は、CVaR 推定誤差の 1-Wasserstein 上界から目的関数と勾配の閉形式が自然に導出され、FQF への変更が係数一つで済む点にあると考えます。

**補足**: 勾配の閉形式は FQF の勾配と同じ形から $g'(\tau_i)$ 因子が付くだけであり、導出は A1 に示すライプニッツ則と包絡項の消去によります。

### Q4. 実験規模（CPU 演算）の妥当性は？（表示: A5）

**回答**: 実験Bは Ryzen 7 5825U の CPU 上で、1 run 約 35 分、100k ステップ、3 シードで実施しました。RiskyChain-v0 は小規模な環境であり、数千エピソードにわたる評価で平均と標準偏差を報告し、統計的有意性を確認しております。より大規模な環境への拡張は、デルタヘッジ環境とあわせて今後の課題と位置づけております。

**補足**: 実験Aは真の分位点関数が既知の解析的な問題であり、数値積分による高精度の基準値との比較で妥当性を確保しています（A3）。

### Q5. 実験B で IQN が MAE で優位だった理由と対策は？（表示: A5）

**回答**: 要因は FPN の非凸最適化に起因する局所解と初期化バイアスであると考えております。初期配置が一様に近く、下側裾への移動が遅れるためで、B1 の標準偏差が大きい点とも整合します。対策として、デルタヘッジ環境では $g$ に基づく初期配置による初期化改善を適用します。なお FQF 比では約 5 分の 1 の誤差低減が得られており、損失側の歪み重み付けの効果は保持されております。

**補足**: IQN は読み出し側で歪みを扱うため表現の配置問題を抱えない一方、本研究は FQF の枠組みで配置を学習できる点を主張しており、比較軸が異なります。

---

## 予備質問（深掘り・追加想定）

### Q6. 上界 $L_g$ の証明の核心は？（表示: A1、必要なら A2）

**回答**: 3段階であります。第一に、歪み分布の期待値が CVaR に一致すること、すなわち $\E[g_\# Z]=\int_0^1 \Finv(g^{-1}(u))\,du=\CVaR_\alpha(Z)$ であります。第二に、期待値の差は 1-Wasserstein 距離で抑えられること $|\E X-\E Y|\le W_1(X,Y)$。第三に、変数変換 $u=g(\tau)$ により、この $W_1$ が重み $g'(\tau)$ 付きの積分 $\Lg$ に書き換わることであります。

**補足**: 最小化の対象は「CVaR 推定誤差の上界そのもの」になるため、$\Lg$ を小さくすれば CVaR の推定誤差が小さくなることが保証されます（本編5枚目の命題）。最適量子化の理論（Graf \& Luschgy）との対応は A2 を参照ください。

### Q7. 裾外の勾配が 0 で FPN は学習が進まないのでは？（表示: A1 下段の注）

**回答**: $g'(\tau_i)=0$ の quantile fraction は $\Lg$ に直接寄与しないのはそのとおりであります。ただし FPN は累積 softmax で quantile fraction を出力するため、裾内の勾配が全ロジットに伝播し、裾外の区切りも間接的に更新されます。実際、実験Bで下側裾への集中が生えていることが確認できております。

### Q8. 原子数 $N$ はどう選ぶべきか？（表示: A3）

**回答**: 実験Aでは $N=16$ を主設定とし、$N$ を変えた感度分析を行いました。改善比 $R$ は $\alpha N\ge 2$ の領域で安定して大きく（最小 $42.3$）、log-log 傾き約 $-1.8$ で伸びることを確認しております。CVaR$_\alpha$ を対象とする場合、下側裾に 2 点以上の原子を置ける $\alpha N\ge 2$（$\alpha=0.05$ なら $N\ge 40$）が一つの目安になると考えております。

**補足**: 点数換算の考え方（歪み最適 16 点 ≈ 一様 695 点）は、同じ誤差を達成するのに必要な一様配置の原子数であり、リソース配分の利得の尺度であります。

### Q9. FPN の非凸性・局所解にはどう対処するのか？（表示: A3・A5）

**回答**: 実験Aでは非凸性に備えてマルチスタートで最良解を採用しており、評価は問題になりませんでした。実験Bでは初期化バイアスが残り、シード間のばらつき（B1 の SD $\pm0.75$）として現れております。対策として、$g$ に基づく初期配置による FPN の初期化改善を、次のデルタヘッジ環境で適用する予定であります。

### Q10. 金融デルタヘッジ環境への適用はどう進めるのか？（表示: 本編10枚目）

**回答**: 卒業研究では、損益分布の下側裾を対象とした実環境評価として、金融デルタヘッジ環境への適用を進めます。あわせて理論の厳密化として一般の歪み $g$（非線形歪み）への拡張と、有限サンプル設定 A-fs での誤差評価を行い、最適化の安定化として FPN の初期化改善を実装する予定であります。

---

## 質疑時の注意

- 回答は30〜45秒で区切り、余計な前置きをしない。
- 数値はすべて本編・Appendix の提示値のみを使用する（その場で新しい数値を言わない）。
- 「オラクル」「テール」「P&L」等の禁止用語は使わない（表記ルールは `task/用語集.md`）。
- 分からない質問には「現時点では検証しておらず、今後の課題と考えております」と答える。
